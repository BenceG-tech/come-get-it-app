import { Stack } from "expo-router";

export default function RewardsCategoryLayout() {
  return (
    <Stack
      screenOptions={{
        headerShown: true,
        headerStyle: { backgroundColor: "#000" },
        headerTintColor: "#fff",
      }}
    />
  );
}
